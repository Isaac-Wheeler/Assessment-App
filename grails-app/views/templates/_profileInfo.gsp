<div class="profileInfo">
    <g:set var="teacheri" value="${session.teacher}" />
    <br>
    <p>${session?.teacher?.firstName} ${session?.teacher?.lastName}<p>
</div><%--info displayed in main from session--%>
